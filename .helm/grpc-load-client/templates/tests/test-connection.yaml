apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "grpc-load-client.fullname" . }}-test-connection"
  labels:
    {{- include "grpc-load-client.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "grpc-load-client.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
